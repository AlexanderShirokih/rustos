[workspace]
resolver = "3"
members = [
    "arch/aarch64",
    "arch/aarch64-paging",
    "arch/common",
    "core", "fdt",
    "memory",
    "util",
    "xtask",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
opt-level = 2
