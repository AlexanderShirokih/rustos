[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = "0.10.0"
drivers-common = { path = "../drivers/common" }
memory = { path = "../memory" }
interrupts = { path = "../interrupts" }
klog = { path = "../log" }
