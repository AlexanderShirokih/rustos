[package]
name = "aarch64-paging"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel = { path = "../../kernel" }
memory = { path = "../../memory" }
collections = { path = "../../collections" }
