[package]
name = "memory"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
default = []
# Включает диагностические API (например, `MemoryMapper::query_l3_raw`),
# нужные интеграционным QEMU-тестам ядра.
qemu-tests = []

[dependencies]
collections = { workspace = true }
klog = { workspace = true }
spin = { workspace = true }

[lints]
workspace = true
